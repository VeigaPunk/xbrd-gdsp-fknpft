---
description: Alias for /xbt — Judge-orchestrated TEAM mode with cross-model delegation and deliberative rounds.
argument-hint: <prompt for the judge, or leave blank to init an empty squad>
allowed-tools: [Agent, Bash, Read, Write, Edit, Glob, Grep, TaskCreate, TaskGet, TaskList, TaskUpdate, SendMessage, TeamCreate, TeamDelete, WebFetch, WebSearch]
---

# This is an alias for /xbt. See /xbt for the full skill definition.

Read `~/.claude/commands/xbt.md` and follow it exactly with `$ARGUMENTS` as the prompt.
