# wwkd-regress-0420 R1 — /wwkd Slash Command + Phase 0 Planner Wiring
**Status:** COMPLETE | **Date:** 2026-04-20 | **Session:** 1

---

## Round Overview

**Mission:** Diagnose why `/wwkd` is broken; reproduce the gap; converge on fix-shape before executor dispatch.

**Axes audited:** W (slash command existence) · D (Phase 0 drift between agent defs and orchestrator templates) · P (on_spawn_skill + skill-load reliability) · S (bun/PATH interference) · B (fix shape: slash-only vs phase-0-only vs combined)

**Teammates (R1):**

| Name | Model | Axis | xask target |
|------|-------|------|-------------|
| the-revenger-wwkd | codex-R-F | W | git log + history.jsonl trail; xask -R -F codex |
| ccs-reviewer-drift | sonnet-medium | D | the-judge.md:35 vs xbgst.md Phase 0 drift audit |
| g-connector-bun | gemini-high | S/cross | PATH audit + topology mismatch + bun bootstrap risk |
| cdx-labrat-planner | codex-spark | P | on_spawn_skill:wwkd load path empirical probe |
| ccs-critic-fixshape | sonnet-medium | B | fix shape adversarial review (4 shapes A/B/C/D) |
| ccs-distiller | sonnet-medium | synthesis | dedup + conflict flag (SYNTHESIS_READY) |
| ccs-scribe-r1 | sonnet-medium | documentation | this report |

**xask targets:** codex-R-F (revenger, full 1.05M ctx), codex-spark (labrat), gemini-high (connector)
**Wall-time:** Concurrent with Pareto scoring; scribe dispatched simultaneously with distiller per protocol

---

## Per-Teammate Findings

### the-revenger-wwkd
**MOVE:** Document wiring gap; provide intent evidence for executor
**AXIS:** W (slash command existence)
**CLAIM:** `/wwkd` slash command never existed in the repo or `~/.claude/commands/`. Dual-mode intent (standalone + composed with `/xbgst`) was present from day one. Commit 6ba6d79 wired the-planner's `on_spawn_skill:wwkd` but Phase 0 planner spawn clause was never written into `commands/xbgst.md`.
**EVIDENCE:**
- `git log --all -- commands/wwkd.md` → empty (no commits across all revisions)
- `~/.claude/history.jsonl:5838-5840` (2026-04-15, session tango/inv-hxh-v3): user coined `/wwkd` = "what would karpathy do command" and "make it a skil also /xbgst" — dual-mode from inception
- Commit `6ba6d79` (2026-04-16): wired the-planner Phase 0 spawn + `on_spawn_skill: wwkd`; `commands/xbgst.md` Phase 0 never received the explicit planner spawn clause — confirmed by revenger across all revisions
- `on_spawn_skill` status: **convention field only** — 2 uses total (critic: heuer-planning, planner: wwkd); CC harness does NOT auto-execute it; agents must issue explicit `Skill()` call; instruction body enforces it manually
**REJECTED ALTERNATIVE:** None stated; scoped to root-cause identification
**CONFIDENCE:** Certain (4 independent evidence sources)

---

### ccs-reviewer-drift
**MOVE:** Patch Phase 0 in all 4 orchestrator templates to add explicit planner spawn clause
**AXIS:** D (Phase 0 drift)
**CLAIM:** `the-judge.md:35` and `commands/references/xbreed-shared.md:106` both name the-planner as "spawned FIRST at Phase 0 by the-judge to map skeleton before specialist dispatch" — but this clause is absent from all orchestrator templates: `xbgst.md`, `xgs.md`, `xbt.md`, `xbreed.md`. Codex-proposed diff would insert Phase 0a planner spawn block into each.
**EVIDENCE:** `commands/xbgst.md` Phase 0 pre-fix: "### Phase 0 — Name the axes" with no planner spawn; `the-judge.md:35` (agent dispatch table, planning row): "CC native — spawn FIRST at Phase 0 to map skeleton baseline before specialist dispatch"; `commands/references/xbreed-shared.md:106`: identical claim; 4 templates all missing the clause (confirmed by grep)
**REJECTED ALTERNATIVE:** Conditional spawn ("only if /wwkd prefix") — rejected; judge hardened to unconditional per the-judge.md:35 language and 6ba6d79 intent
**CONFIDENCE:** Certain; blocker severity

---

### g-connector-bun
**MOVE:** (1) Flag /wwkd as topology mismatch, not PATH issue; (2) flag bun personal-plugin bootstrap as latent risk
**AXIS:** S (bun interference) · cross-axis pattern
**CLAIM:** PATH audit clean — bun is 5th entry, no shadowing of CC tools. The `/wwkd` failure is an `on_spawn_skill` topology mismatch (slash trigger inert, planner dispatch absent), not a bun interference issue. Separate latent: `~/.claude/personal-plugin/` bootstrap may use bun for skill auto-install → silent failure vector if bun errors at CC startup.
**EVIDENCE:** `echo $PATH` bun position: 5th entry, after `/home/vhpnk/.local/bin`, no tool shadowing. `~/.claude/personal-plugin/` exists (verified by scribe: `ls` → `skills/` only, no `package.json`). Bun install bootstrap path moot for this environment.
**REJECTED ALTERNATIVE:** Bun as proximate cause — ruled out by PATH audit
**CONFIDENCE:** Strong (PATH finding certain; bun-plugin risk downgraded to latent — no package.json present)

---

### cdx-labrat-planner
**MOVE:** Flag on_spawn_skill as unverifiable witness; flag skill-load.log gap
**AXIS:** P (on_spawn_skill reliability)
**CLAIM:** `on_spawn_skill:wwkd` on the-planner is dead metadata — CC harness does NOT auto-fire it. `skill-load.log` was empty in mission bench-stackchar-mC (2026-04-17): unknown whether never wired vs regression. Affects confidence in all prior "Skill(wwkd) honored" observations.
**EVIDENCE (scribe-verified):** `git log --all --oneline -- skill-load.log` → empty (no commits touching that file — **never wired**, not a regression). `docs/reports/bench-stackchar-mC-r1-2026-04-17.md` line: "`skill-load.log` | 0 | No entries — skill-load witness gap". Corroborated by revenger's finding that `on_spawn_skill` is convention-only across 2 uses.
**REJECTED ALTERNATIVE:** skill-load.log as regression — ruled out by git log
**CONFIDENCE:** Certain (never-wired confirmed)

---

### ccs-critic-fixshape
**MOVE:** Reject fix shape C's A-component as implemented; prefer B alone or corrected C
**AXIS:** B (fix shape adversarial)
**CLAIM:** Four shapes surveyed — A (create `commands/wwkd.md` reading SKILL.md directly), B (Phase 0 planner dispatch unconditional in orchestrators only), C (A+B), D (strip `on_spawn_skill` field or wire it). Critic: RETHINK C's A-component. `commands/wwkd.md` never existed — "restore" framing is incorrect. A as proposed reads SKILL.md directly, bypassing `Skill()` infrastructure — divergent from the-planner's Layer-0 load path, creating two behavioral profiles for the same posture. B alone is surgical, zero new surface.
**EVIDENCE:** (1) `git log --all -- commands/wwkd.md` → empty (no prior version to restore). (2) Proposed A-component: `Read ~/.claude/skills/wwkd/SKILL.md` inline — bypasses `Skill(skill="wwkd")` call that the-planner uses at Layer 0. (3) Two behavioral profiles: `/wwkd` (in-session, no planner) vs `/xbgst` + planner path.
**REJECTED ALTERNATIVE:** Shape A as proposed — divergent profile risk. Shape D (strip on_spawn_skill) — R2 candidate, not R1 scope.
**CONFIDENCE:** Strong

---

## Cross-Model Conflicts + Judge Resolution

### CONFLICT: Fix shape C — A-component behavioral divergence

| Position | Agent | Evidence |
|----------|-------|----------|
| A-component: Read SKILL.md directly | (implicit in early C proposals) | Simpler implementation |
| A-component: Skill() call | ccs-critic-fixshape | Unifies /wwkd with the-planner Layer-0 load; eliminates dual behavioral profiles |

**Judge Resolution:** ADOPTED CORRECTED C — both M1 (commands/wwkd.md) and M2 (Phase 0 unconditional planner) implemented. A-component hardened: `commands/wwkd.md` uses `Skill(skill="wwkd")` with explicit comment "Do NOT manually Read the file; the Skill() tool is the canonical load path." Critic's divergence risk closed.

### PROTOCOL ERRATA: Critic brief missing propose-only clause

Judge brief for `ccs-critic-fixshape` omitted the "propose diff, do NOT apply" clause. Codex applied edits in-place. Second offense per `memory/feedback_reviewer_brief_propose_only.md` (first: honcho-reaudit A2, 2026-04-18). Logged; not a blocker for R1 scope.

---

## Pareto Verdict per Move

| Move | Axis | Verdict | Rationale |
|------|------|---------|-----------|
| M1: Create `commands/wwkd.md` via `Skill()` | W | **ACCEPTED** | Closes /wwkd inert trigger; Skill() path unifies behavioral profile with the-planner Layer-0 |
| M2: Phase 0 unconditional planner in all 4 orchestrators | D | **ACCEPTED** | Closes drift between the-judge.md:35 and xbgst/xgs/xbt/xbreed Phase 0; no regression surface |
| M3: on_spawn_skill finding — no action R1 | P | **DEFERRED R2** | Never wired (not regression); strip or wire field is low-priority cleanup candidate |
| M4: Bun non-interfering | S | **CLOSED** | PATH audit clean; user self-suspicion ruled out; bun-plugin risk moot (no package.json) |
| M5: Protocol errata (propose-only) | — | **LOGGED** | Judge procedure gap, not a code change; memory entry pre-exists |

---

## Optimization Routes Surveyed (Fix Shapes A/B/C/D)

| Shape | Description | Verdict |
|-------|-------------|---------|
| **A** | Create `commands/wwkd.md` reading SKILL.md via `Read` directly (standalone /wwkd) | REJECTED — bypasses Skill() infra; creates dual behavioral profiles for same posture |
| **B** | Phase 0 planner dispatch unconditional in orchestrators only; no new slash command | VALID — surgical, zero new surface; does not close /wwkd standalone trigger |
| **C** | A + B combined (slash command + Phase 0 restore) | ADOPTED with A-component hardened to use Skill() not Read |
| **D** | Strip `on_spawn_skill` dead metadata field, or wire CC harness to honor it | DEFERRED R2 — scope expansion; low-priority cleanup |

**Winning shape:** C (hardened) — closes both the standalone trigger gap and the orchestrator Phase 0 drift in one round. A-component correction (Skill() over Read) is the key hardening that made C viable.

---

## Distiller SPOOF FLAGS (SYNTHESIS_READY arrived post-commit — verbatim relay)

1. **Revenger git-log-is-empty claim: partial spoof.** `git log` on the repo returned `3f790fd`. The underlying finding is correct (no commits touch `commands/wwkd.md` specifically), but the claim was imprecisely scoped — "git log is empty" is false; `git log -- commands/wwkd.md` is empty.
2. **Judge orientation stale at commit time.** Working tree already had M1+M2 applied uncommitted when distiller DMs landed. Scribe wrote from evidence in hand per protocol; distiller's late arrival confirmed alignment.

## audit_hash

`4c55cdc7148dbfca9c05c5a03a6665537f123e8741a182c2fea1d980eeaf2432`
(canonical — from ccs-distiller SYNTHESIS_READY, second message)

---

## Commit Delta

| File | Change |
|------|--------|
| `commands/wwkd.md` | CREATED — /wwkd slash trigger; `Skill(skill="wwkd")` load; composition docs |
| `commands/xbgst.md` | M2 — Phase 0 section rewritten to unconditional planner spawn with Agent() example |
| `commands/xgs.md` | M2 — Phase 0 planner spawn added (unconditional) |
| `commands/xbt.md` | M2 — Phase 0 planner spawn added (unconditional) |
| `commands/xbreed.md` | M2 — Phase 0 planner spawn added (unconditional) |
| `scripts/install-commands.sh` | wwkd added to symlink loop |
| `docs/command-flows.md` | /wwkd entry + composition docs added |

**Gate:**
```bash
for check in \
  '[ -f commands/wwkd.md ]' \
  'grep -q "Skill(skill=\"wwkd\")" commands/wwkd.md' \
  '! grep -q "Read.*SKILL.md" commands/wwkd.md' \
  'grep -q "Planner-first is unconditional" commands/xbgst.md' \
  'grep -q "Planner-first is unconditional" commands/xgs.md' \
  'grep -q "Planner-first is unconditional" commands/xbt.md' \
  'grep -q "Planner-first is unconditional" commands/xbreed.md' \
  'grep -q "wwkd" scripts/install-commands.sh' \
  'cargo test 2>&1 | grep -q "2 passed; 0 failed"'; do
  eval "$check" && echo "PASS" || echo "FAIL: $check"
done
```
Expected: 9× PASS
Actual: 9× PASS

## Links
- Plan: (no formal plan artifact — judge-oriented, mission self-framed)
- Prior mission: xask-gate-regress-0420 (d8f2af3 / 5979074 / 8e37064 / 3f790fd)
- Next: M3 candidate R2 — strip `on_spawn_skill` dead metadata or wire CC harness
