# xbrd-vs-hvm4 R1 Audit Trail
**Team:** xbrd-vs-hvm4-0423 | **Date:** 2026-04-23 | **Mission:** Comparative Pareto walk — xbrd-gdsp-fknpft vs HVM2/Bend (planner Phase 0 initial label "HVM4" was wrong; scout-tech amendment confirmed HVM4 = separate pre-launch C rewrite, 85 stars, no Bend frontend — used HVM2/Bend as primary comparison target throughout R1)

---

## 1. Round Overview

**Wall-time:** R1 (single session, 2026-04-23)

**User brief (verbatim):** _"compare our approach to HVM4 through extensive usage of scouts and mutation testers to show how xbrd-gdsp-fknpft is the REAL G THANG /xbgst /wwkd | godspeed"_

**Framing correction (Phase 0):** ccs-planner-r0 issued WWKD data-walk; g-scout-hvm-tech returned amendment — HVM4 ≠ Higher-order Virtual Machine 2. HVM4 = separate C-language pre-launch project (85 GitHub stars, no Bend frontend, no public release). Comparison target realigned to HVM2 + Bend (the Rust-implemented runtime + parallel functional language publicly shipped by HigherOrderCO). Honest-but-polemical framing confirmed.

### Teammate Roster

| Teammate | Model / Route | Axes | Outcome |
|---|---|---|---|
| ccs-planner-r0 | CC native (sonnet-medium) | Phase 0 WWKD | COMPLETE — WWKD data-walk + 8-axis rubric issued |
| g-scout-hvm-tech | gemini medium → xask BLOCKED → Layer-3 in-session WebFetch fallback | A2 (HVM tech), A4 (UX) | COMPLETE (fallback path) — primary xask call blocked 300s timeout |
| g-scout-hvm-community | gemini medium | A6 (community), A7 (roadmap/self-claims) | COMPLETE |
| g-mutation-tester-xbrd | gemini low fanout → xask fanout BLOCKED (300s timeout) | A2 (xbrd rigor) | PARTIAL GAP — pre-finding logged (baked-in mutation in protocol.rs:72-76 discovered and reverted); guard.rs / mailbox mutations NOT completed |
| g-connector-r1 | gemini high (90s reasoning cap respected) | Cross-axis R1-C | COMPLETE |
| cdx-critic-framing | heuer Layer-0 → codex -R | A1 (framing adversarial audit) | COMPLETE |
| cdx-reviewer-claims | codex -R (propose-only) | Citation audit all axes | COMPLETE → DESPAWNED post-task |
| ccs-distiller-r1 | CC native | Synthesis | COMPLETE — 18 moves, SYNTHESIS_READY |

---

## 2. Per-Teammate Detail

### 2.1 ccs-planner-r0 — WWKD Phase 0

**MOVE:** Issued 8-axis rubric (A1–A8) and WWKD data-walk anchoring xbrd vs HVM2/Bend comparison. Initial Phase 0 brief included "HVM4" label from user prompt.

**Evidence:** Phase 0 produced axis definitions:
- A1: Framing / scorecard discipline
- A2: Technical rigor (tests, mutation coverage, correctness)
- A3: Safety policy
- A4: UX / operability
- A5: Interop / FFI
- A6: Community health
- A7: Roadmap / self-claim honesty
- A8: Benchmarks / perf rubric

**Rejected alternative:** Accepting "HVM4" label at face value without scout verification (would have generated comparative claims against an unreleased 85-star C project — meaningless comparison).

**Peer DMs sent:** Brief to g-scout-hvm-tech requesting HVM identity verification as first task. Amendment received; rubric locked on HVM2/Bend.

---

### 2.2 g-scout-hvm-tech — HVM2 Tech + A4 UX

**MOVE (A2 HVM):** Primary-sourced: HVM2's 64-bit runtime was never shipped; `hvm-64` repository ARCHIVED on GitHub. All benchmarks and community hype anchored to HVM1 (32-bit). HVM2 is a ground-up Rust rewrite with a divergent architecture; benchmark claims in HN threads refer to the old codebase.

**MOVE (A4):** HVM/Bend UX: Bend compiler errors on common GPU-targeting patterns; `--cuda` flag requires CUDA toolkit installed globally; no fallback to CPU-parallel mode documented in README. End-user stranded category: medium-high.

**Evidence:**
- `hvm-64` repo: GitHub shows "Archived" badge, 0 releases, last commit 2023-09. (WebFetch Layer-3 fallback — xask call timed out at 300s)
- Bend README: `--cuda` flag listed without fallback documentation (WebFetch Layer-3)
- HVM4 identity: separate repo `HigherOrderCO/hvm4`, 85 stars, C source, no releases, no Bend integration (WebFetch Layer-3)

**Rejected alternative:** Treating HVM2 Rust rewrite as equivalent to HVM1 benchmarks (false continuity — different runtime, different performance profile, no shared benchmark corpus).

**Peer DMs:** Received brief from planner. DM sent to distiller: amendment on HVM4 identity + hvm-64 archived status.

**Note:** xask gemini medium call BLOCKED (300s timeout). All findings via Layer-3 in-session WebFetch fallback. Confidence on tech claims: medium (not primary-source git clone; screenshot/page read only).

---

### 2.3 g-scout-hvm-community — A6 Community + A7 Roadmap

**MOVE (A6):** Cross-model PR/Issue analysis: HVM2 GitHub shows 47 open issues with "help wanted" tag, 3 open PRs stale >90 days. Bend has 89 open issues, last maintainer response on bug reports: 11 days median (as of scout date). Community health: active but thin — two primary contributors account for >80% of commits (dated GitHub snapshot).

**MOVE (A7):** HVM roadmap/self-claims audit: HigherOrderCO README claims "optimal beta-reduction," "automatic parallelism," "GPU-native." Scout finding: "automatic parallelism" requires explicit `fold`/`bend` syntax — not transparent. "GPU-native" requires CUDA toolkit. "Optimal beta-reduction" is a theoretical claim (interaction nets) without empirical benchmark in repo. A7 scoring: self-claims are partially buried — disclaimer requires reading paper, not README.

**Evidence:**
- GitHub issue tracker: 47 "help wanted" open (HVM2), 89 open (Bend) — cross-model PR/Issue + commit dates per distiller synthesis
- Contributor concentration: Pareto of commits (gemini medium direct web search)
- README section "How to use": `--cuda` listed as primary path; CPU fallback in footnote only

**Rejected alternative:** Accepting HigherOrderCO marketing copy at face value for A7 self-claim scoring.

**Peer DMs:** DM sent to distiller with A6/A7 findings. No conflicts received from peers on these axes.

---

### 2.4 g-mutation-tester-xbrd — A2 xbrd Rigor (PARTIAL GAP)

**STATUS: PARTIAL GAP — pre-finding only. xask fanout call BLOCKED (300s timeout). guard.rs and mailbox.rs mutations NOT completed.**

**Pre-finding (ACCEPTED as partial move):** During protocol setup, mutation tester discovered a baked-in mutation in `src/protocol.rs` lines 72-76: the EOF flush block (`if let Some(h) { sections.push((h, body_count)); }`) had been replaced with a comment `// MUTATION: Removed EOF flush`. This is not a live test-injected mutation — it was present in the committed source. Reverting restores the final heading push, which is required for correct section parsing when the last section has no trailing newline.

**Before (mutated):**
```rust
// MUTATION: Removed EOF flush
// if let Some(h) = current_heading {
//     sections.push((h, body_count));
// }
sections
```

**After (restored):**
```rust
if let Some(h) = current_heading {
    sections.push((h, body_count));
}
sections
```

**Red-before-green evidence:**
- Pre-revert: `cargo test --release --lib` → 68/70 passed (2 failures in `parse_sections_*` tests requiring EOF flush)
- Post-revert: `cargo test --release --lib` → 70/70 passed

**Full post-revert test output tail:**
```
test protocol::tests::parse_sections_ignores_fenced_heading_lookalikes ... ok
...
test result: ok. 70 passed; 0 failed; 0 ignored; 0 measured; 0 filtered out; finished in 1.21s
```

**Planned mutations NOT completed:** guard.rs deny-list bypass mutations, mailbox.rs race-condition mutations. Logged as R2 gap.

**Evidence for xbrd test count:** `cargo test` output: 70 tests (contradicts planner Phase 0 estimate of 33 — SPOOF FLAG M7, see §6).

**Peer DMs:** Pre-finding relayed to distiller immediately. Gap status logged.

---

### 2.5 g-connector-r1 — Cross-Axis R1-C

**MOVE (R1-C):** Cross-axis pattern match on full 8-axis table. Key finding: A3 (safety policy) and A5 (FFI/interop) are category-orthogonal to HVM2's domain. HVM2 is a language runtime, not a control plane — comparing xbrd's OAuth deny-list policy to HVM2's safety posture is a category error. Connector recommendation: DROP A3 and A5 from scorecard, or explicitly re-label as "control-plane-specific axes not applicable to HVM2."

**Secondary finding (R1-C):** A1 frame weakness compounds if A3 stays. A gerrymandered scorecard (axes xbrd wins by construction) + A1 "bilateral uncited burden" creates a double fragility. Connector flagged: even if each individual axis reading is accurate, the axis selection pattern reads as adversarial.

**Evidence:** Full table scan — g-connector-r1 applied reasoning cap (90s), returned structured finding within cap. No timeout.

**Rejected alternative:** Keeping A3 and A5 without re-labeling (would inflate apparent xbrd advantage on non-comparable dimensions).

**Peer DMs:** Distiller received R1-C via DM. A3 and A5 marked DROP in Pareto verdict.

---

### 2.6 cdx-critic-framing — A1 Framing Adversarial Audit

**MOVE (A1):** Heuer Layer-0 structured adversarial critique of the 8-axis scorecard. Key finding: baseline A1 claim ("scorecard is neutral") is self-refuting — a scorecard designed by the project being compared cannot be neutral by construction. Heuer diagnosis: "mirror-imaging bias." Revised A1 framing: acknowledge adversarial posture explicitly or reframe comparison domain.

**Honest reframe proposed (load-bearing for R2):** "xbrd is a control plane — its natural comparators are k8s/ray/supervisor frameworks, not CUDA runtimes or language VMs. Comparing to HVM2 is a category-adjacent move that illuminates what xbrd is NOT, but cannot score xbrd positively on HVM2's native axes (beta-reduction efficiency, GPU utilization)."

**A1 bilateral uncited burden:** cdx-critic-framing flagged that A1 weakness is bilateral — not just xbrd's claims lack citations; the scorecard's own framing claims also lack citations. Both sides need anchoring.

**Evidence:** codex -R heuer structured output (propose-only, no edits applied). Findings relayed verbatim to distiller.

**Peer DMs:** A1 findings sent to distiller. Critic meta-critique relayed (see §9).

---

### 2.7 cdx-reviewer-claims — Citation Audit (DESPAWNED)

**MOVE:** Propose-only citation audit across all 18 distiller moves. Two SPOOF FLAGS surfaced (see §6). Six citations flagged as FIXABLE (permalinks missing/soft). No moves rejected on citation grounds beyond spoof flags.

**FIXABLE citations identified:**
1. HN thread 40390287 — referenced by name, no permalink anchored
2. GitHub "No releases" for HVM2 — needs dated snapshot URL
3. `policy.yaml` for A3 scope-lock claim — needs file:line reference
4. A7 "buries disclaimer" editorial — needs README section heading quote
5. xbrd OAuth exclusivity — no commit hash cited
6. Deny-list coverage — no test name or count cited

**Evidence:** Propose-only codex -R output. No edits applied. Citations audit delivered to distiller.

**Status:** DESPAWNED post-task per judge protocol (no further citation work in R1 scope).

---

### 2.8 ccs-distiller-r1 — Synthesis

**MOVE:** Synthesized 18 moves from 7 teammates. Applied EVIDENCE AUDIT. Issued SYNTHESIS_READY with audit_hash.

**18 moves summary:**

| Move | Axis | Accept/Reject | Notes |
|---|---|---|---|
| M1 | A1 | ACCEPT | Baseline → "self-refuting ceiling + bilateral uncited burden" |
| M2 | A1 | ACCEPT | Honest reframe: xbrd = control plane, not VM competitor |
| M3 | A2 (xbrd) | ACCEPT | 70 tests confirmed (not 33) |
| M4 | A2 (xbrd) | ACCEPT | 1 mutation reverted R1 (protocol.rs EOF flush) |
| M5 | A2 (xbrd) | ACCEPT | 1 known-race mailbox test flaky, passed this run |
| M6 | A2 (HVM) | ACCEPT | hvm-64 ARCHIVED, 64-bit never shipped |
| M7 | A2 (xbrd) | ACCEPT + SPOOF FLAG | Planner "33 tests" contradicted by direct cargo test (70). False attribution — planner Phase 0 stated 33 without running tests. |
| M8 | A3 | DROP | Category-orthogonal per connector R1-C |
| M9 | A4 | ACCEPT | HN verbatim anchored, two-level scoring (project iteration vs end-user stranded) |
| M10 | A4 | ACCEPT | Bend `--cuda` no fallback documented |
| M11 | A5 | DROP (M13 negative finding) | |
| M12 | A5 | DROP (M13 negative finding) | |
| M13 | A5 | ACCEPT (negative) + SPOOF FLAG | "no FFI"/"no tensor cores" attribution to g-scout-hvm-tech is FALSE — those claims did NOT originate in scout-tech output; true origin unverified |
| M14 | A6 | ACCEPT | Cross-model PR/Issue + commit dates anchored |
| M15 | A6 | ACCEPT | Contributor concentration (2 primary >80% commits) |
| M16 | A7 | ACCEPT (medium confidence) | HVM4 pivot narrative — Bend2/NeoGen NOT a repo |
| M17 | A7 | ACCEPT | Self-claim audit: "automatic parallelism" requires explicit syntax |
| M18 | A8 | DROP | No rubric defined — cannot score |

---

## 3. EVIDENCE AUDIT Line

```
EVIDENCE AUDIT: 18 moves with evidence, 0 without, 0 dropped, 2 spoof_flagged
  (M7 exposes planner 33→70 test miscount; M13 exposes A5 "no FFI"/"no tensor cores" false attribution)
```

---

## 4. Pareto Verdict Table

All 18 moves ACCEPT as strict improvers over pre-walk baseline. Axis outcomes:

| Axis | Pre-Walk Baseline | Post-R1 State | Net Move |
|---|---|---|---|
| A1 (Framing) | "Neutral scorecard" (self-refuting) | Bilateral uncited burden acknowledged; honest reframe proposed | +2 moves ACCEPT |
| A2 xbrd (Tech rigor) | "33 tests claimed" | 70 tests confirmed, 1 mutation reverted, 1 flaky race known | +3 moves ACCEPT |
| A2 HVM (Tech rigor) | HVM4/HVM2 conflated | hvm-64 ARCHIVED, 64-bit never shipped, HVM4 = separate project | +2 moves ACCEPT |
| A3 (Safety) | In scope | DROP — category-orthogonal (control plane vs runtime) | DROP |
| A4 (UX) | Uncited | HN verbatim, two-level scoring, --cuda no-fallback documented | +2 moves ACCEPT |
| A5 (Interop/FFI) | In scope | DROP — M13 negative finding; attribution to scout FALSE | DROP (×3: M11, M12, M13) |
| A6 (Community) | Uncited | Cross-model PR/Issue, commit dates, contributor concentration | +2 moves ACCEPT |
| A7 (Roadmap) | Uncited | Self-claim audit anchored, HVM4 pivot narrative (medium) | +2 moves ACCEPT |
| A8 (Benchmarks) | In scope | DROP — no rubric | DROP |

**Active axes post-R1:** A1, A2 (both sides), A4, A6, A7
**Dropped axes:** A3, A5, A8

---

## 5. Cross-Model Conflicts + Judge Resolutions

### Conflict 1: A1 vs A7 Frame Incompatibility
**g-connector-r1** (R1-C): A7 self-claim audit requires accepting HVM2's framing as a reference point, which contradicts A1 finding that scorecard axes are adversarially selected.

**Resolution (distiller inline):** Axis-scoped. A7 = auditing HVM's own self-claims against HVM's own evidence (does NOT require scorecard neutrality). A1 = auditing scorecard's own claim-evidence discipline. The two axes operate on different objects. No logical conflict when scoped correctly.

### Conflict 2: A4 Severity (False Conflict)
**g-scout-hvm-tech** (WebFetch): Bend UX rated medium severity.
**g-scout-hvm-community** (community thread synthesis): User reports rated medium-high.

**Resolution (distiller inline):** Not a true cross-model conflict. Both sources converge on two-level scoring (project iteration complexity vs end-user stranded risk). Medium + medium-high = two-level framework adopted, not a contradiction.

---

## 6. Spoof Flags

### SPOOF FLAG M7
**Source:** cdx-reviewer-claims (codex -R, propose-only)
**Claim audited:** ccs-planner-r0 Phase 0 stated "33 tests repo-verified"
**Finding:** Direct `cargo test` output shows **70 tests**. Planner never ran tests — stated count was an estimate or stale read.
**Classification:** False attribution — not a gap, a flip. Planner asserted a number without running the gate command.
**Disposition:** M7 ACCEPT as spoof flag. Score for A2 xbrd updated to reflect 70 tests.

### SPOOF FLAG M13
**Source:** cdx-reviewer-claims (codex -R, propose-only)
**Claim audited:** A5 axis attributed "no FFI" and "no tensor cores" claims to g-scout-hvm-tech output
**Finding:** Those specific claims do NOT appear in g-scout-hvm-tech's WebFetch-fallback findings. True origin unverified — likely training-data extrapolation by the move author.
**Classification:** False attribution — specific language not traceable to named source.
**Disposition:** A5 axis DROPPED (M11, M12 also DROP). M13 ACCEPT as negative finding / spoof confirmation.

---

## 7. R1 Code Delta

### File: `src/protocol.rs` — EOF flush revert

**Context:** g-mutation-tester-xbrd discovered a baked-in mutation (not a live test injection) at lines 72-76. The EOF flush block that pushes the final section when no trailing newline follows had been replaced with a commented-out "MUTATION" block. This caused `parse_sections` to silently drop the last section in any input ending without a newline.

**Before (mutated state in committed source):**
```rust
        // MUTATION: Removed EOF flush
        // if let Some(h) = current_heading {
        //     sections.push((h, body_count));
        // }
        sections
    }
```

**After (restored):**
```rust
        if let Some(h) = current_heading {
            sections.push((h, body_count));
        }
        sections
    }
```

**File:** `src/protocol.rs` lines 72–76 (post-revert: 72–74 active, 75 `sections`)

**Red evidence (pre-revert):** `cargo test --release --lib` → `68 passed; 2 failed`
- `protocol::tests::parse_sections_basic` — FAIL (last section not returned)
- `protocol::tests::parse_sections_ignores_fenced_heading_lookalikes` — FAIL (same root cause)

**Green evidence (post-revert):**
```
test protocol::tests::parse_sections_ignores_fenced_heading_lookalikes ... ok
...
test result: ok. 70 passed; 0 failed; 0 ignored; 0 measured; 0 filtered out; finished in 1.21s
```

**Baseline shift:** 68/70 → 70/70 after revert.

---

## 8. Known-Issue Disclosure: Mailbox Race

Per `project_mailbox_fd_cache_constraint` memory entry:

> Mailbox fd-cache incompatible with async compact. H12-class fd-caching requires compact-path serialization; async compact (Option E) widens stale-inode window → silent data loss without invalidation gate.

One mailbox-related test is known-flaky due to this race condition. It **passed** in R1's `cargo test` run (70/70). It is **not blocking R1** but is a known non-deterministic failure path. Logged for R2 awareness. Mutation testing of mailbox.rs deferred to R2 (mutation-tester did not reach this target in R1).

---

## 9. Critic Meta-Critique on Rubric Design

**Source:** cdx-critic-framing (heuer Layer-0 → codex -R), relayed verbatim.

> "The 8-axis rubric may itself be adversarial by construction. Even if every axis reads true individually, a gerrymandered scorecard reads as a hit piece. The selection of axes that xbrd wins by construction (OAuth exclusivity, deny-list policy, CLI routing) against a runtime that has no analogous concepts creates an apples-to-chainsaws comparison."

**Heuer diagnosis:** Mirror-imaging bias — scorecard designer assumed HVM2 operates in the same problem space as xbrd. It does not. HVM2 is a language runtime targeting GPU parallelism. xbrd is a CLI orchestration control plane. The Venn diagram of their responsibilities has minimal overlap.

**Proposed honest reframe (load-bearing for R2):** Either:
1. **Reframe scorecard:** Explicit statement that axes A3/A5/A8 are inapplicable to HVM2 by design; score only A1/A2/A4/A6/A7 with that caveat explicit. OR
2. **Own the adversarial posture explicitly:** State upfront: "xbrd-gdsp-fknpft is not a HVM2 replacement; this comparison demonstrates what xbrd is by showing what it is not. The axes where xbrd wins are precisely the axes where xbrd's problem domain lives — that is the point."

**R2 requirement:** Judge must choose one of these framings and anchor it before the comparative essay deliverable (the REAL G THANG artifact) is drafted.

---

## 10. Open Gaps (Full R1 List)

1. **xbrd test-suite mutation survival rate** — mutation-tester xask fanout BLOCKED (300s). guard.rs and mailbox.rs mutations NOT tested. Need R2 re-dispatch: scoped to guard.rs only, single-mutation codex-spark path (not gemini fanout).

2. **A5 origin unresolved** — cdx-critic-framing never established who introduced "no FFI"/"no tensor cores" claims. cdx-reviewer-claims flagged as spoof but true origin unverified. May be planner training-data extrapolation. R2: grep Phase 0 brief verbatim for these strings.

3. **A8 rubric undefined** — "Benchmarks / perf rubric" axis was in Phase 0 but no benchmark methodology was defined before scout dispatch. Cannot score without rubric. R2: judge defines benchmark rubric or permanently drops A8.

4. **xbrd control-plane positive-evidence citations missing** — A1 bilateral uncited burden applies to xbrd's own claims:
   - OAuth exclusivity: no commit hash cited (src/ask.rs API-key removal commit)
   - Deny-list coverage: no test name or count cited
   - Routing correctness: no test name cited
   R2: citation closure required before comparative essay.

5. **A6 FIXABLE citations (6 total):**
   - Permalink for HN thread 40390287
   - Dated GitHub snapshot URL for HVM2 "No releases" / hvm-64 Archived
   - `policy.yaml` file:line for A3 scope-lock (if A3 is ever restored)
   - README section heading quote for A7 "buries disclaimer"
   - xbrd OAuth exclusivity commit hash
   - Deny-list test name/count

6. **Scorecard adversarial posture unresolved** — critic meta-critique is load-bearing. Judge must commit to reframe vs explicit-adversarial before R2 essay draft.

7. **g-scout-hvm-tech fallback confidence** — all HVM tech findings via WebFetch Layer-3 (xask BLOCKED). Confidence medium. R2: retry xask gemini medium with shorter timeout or use codex -R WebFetch path for verification.

---

## 11. R2 Agenda Preview

**Priority order:**

1. **Judge framing decision** — reframe scorecard (control-plane-native axes only) OR own adversarial posture explicitly. Must precede essay draft.

2. **Citation closure** — every R1 move with confidence=medium → permalink + line anchor. Six FIXABLE citations from cdx-reviewer-claims + xbrd control-plane positive citations (OAuth commit hash, deny-list tests, routing correctness tests).

3. **Mutation-tester retry (scoped)** — guard.rs only, single-mutation codex-spark path (not gemini fanout). Target: deny-list bypass mutation. Expected: test catches it → A2 xbrd confidence HIGH.

4. **A5 origin grep** — search Phase 0 brief and scout output verbatim for "no FFI" / "no tensor cores" strings. Determine if this was planner extrapolation or scout injection. Close spoof M13 with definitive source attribution.

5. **A8 decision** — judge defines perf benchmark rubric (wall-time xask vs bend compilation, e.g.) OR permanently drops A8 from scorecard.

6. **Draft the comparative essay** — "the REAL G THANG artifact." Target: 1500–2500 words. Structure: honest reframe (§1), xbrd technical rigor case (§2), HVM2/Bend technical findings (§3), axis-by-axis verdict (§4), honest limitations (§5), why xbrd wins in its problem domain (§6). Tone: polemical but primary-sourced.

7. **g-scout-hvm-tech retry** — xask gemini medium with explicit 120s timeout cap, or codex -R WebFetch for hvm-64 archived status confirmation.

---

## 12. Audit Hash + Verification Note

**audit_hash:** `621ba3e5a2fef3a0e30cb4e646fc0d4f08ccbd0754ff0a4b352f7b9b8a94bfc3`

**Verification:** Hash was computed by ccs-distiller-r1 over the 18-move synthesis payload prior to SYNTHESIS_READY signal. Scribe verified hash matches distiller's commit message value. Per Judge Blinding Protocol Step 3, provisional scores stand — no post-hash score amendments in R1.

**Hash covers:** 18 move records (axis, direction, evidence_cite, confidence, spoof_flag). Does NOT cover this report (report generated post-synthesis).

---

## 13. Optimization Routes Surveyed (Considered and Rejected)

1. **HVM4 as primary comparison target** — REJECTED (Phase 0). HVM4 = pre-launch C project, 85 stars, no public release, no Bend frontend. Comparing to an unreleased project would be unfalsifiable. Comparison realigned to HVM2/Bend (shipped, benchmarkable, community-verified).

2. **Gemini fanout for mutation testing** — REJECTED mid-run (g-mutation-tester-xbrd). Fanout call timed out at 300s. Reference: `reference_gemini_fanout_skill.md` — gemini fanout scoped to mutation-tester with ≥5 targets. R1 had only 3 target files. Route mismatch → should have used single-target codex-spark path. Logged as R2 correction.

3. **Keeping A3 (Safety policy) in scorecard** — REJECTED (connector R1-C). Category-orthogonal: xbrd's OAuth deny-list vs HVM2's safety posture is comparing a CDN firewall to a compiler — different layers, different threat models. Keeping A3 would inflate xbrd's apparent advantage on a dimension HVM2 has no analogue for.

4. **Accepting planner's "33 tests" count** — REJECTED (M7 spoof). Direct `cargo test` output is authoritative over Phase 0 estimation. 70 tests confirmed.

5. **Accepting A5 ("no FFI") attribution to g-scout-hvm-tech** — REJECTED (M13 spoof). Scout-tech output (WebFetch Layer-3) does not contain those strings. Attribution is false. A5 dropped rather than propagate a fabricated claim.

6. **Using xask Layer-1 for all scouts concurrently** — ATTEMPTED, partially BLOCKED. g-scout-hvm-tech and g-mutation-tester-xbrd both hit 300s xask timeout. Root cause: gemini medium + gemini low both bottlenecked on same concurrent session slot. R2 mitigation: stagger xask calls or use explicit timeout caps in briefs.

7. **Keeping A8 (Benchmarks)** — REJECTED (M18). No benchmark rubric was defined in Phase 0. Without a rubric, any benchmark score is uninterpretable. Permanently deferred to R2 with judge-definition requirement.

---

*Report generated by ccs-scribe-r1 | Team xbrd-vs-hvm4-0423 | 2026-04-23*
*1:1:1 invariant: this report covers R1 only. R2 will have a separate report.*
