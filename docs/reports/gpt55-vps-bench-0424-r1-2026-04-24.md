# gpt55-vps-bench-0424 — Round 1 Report
**Date:** 2026-04-24 | **Team:** gpt-5.5-vps-bench-0424 | **Axis family:** documentation

---

## Round 1 Overview

**Mission:** Fill a 4-effort × fast-on/off × n=3 = 24-measurement table for gpt-5.5 on VPS hermes-node (104.236.209.135) via xbreed bench-cell.sh.

**Axes named (7):** empirical reachability, correctness methodology, VPS execution, cross-axis reachability parity, variance control, template fidelity, VPS parity.

**Teammates dispatched:** `ccs-planner-r0` (Phase 0), `cdx-labrat-gpt55-probe`, `cdx-reviewer-bench-methodology`, `cdx-executor-vps-bench`, `g-connector-bench-crosspattern`.

**xask targets:** `xask --spark codex` (labrat/executor), `xask -R codex` (reviewer), `xask --effort high gemini` (connector — timed out, Layer 3 fallback).

**Wall time:** ~30 min (team created 14:41, executor MOVE delivered ~15:06).

---

## Per-Teammate MOVE / AXIS / CLAIM / EVIDENCE

### ccs-planner-r0 (Phase 0)
- **MOVE:** Skeleton plan — raw-arm bypasses xask; zero src/ changes needed; sequence S1→S1b→M1→M2→M3.
- **Evidence:** Primary-source reads of `bench-cell.sh`, `ask.rs`, `Makefile`.
- **Verdict:** ACCEPTED.

### cdx-labrat-gpt55-probe
- **MOVE M2:** Reachability confirmed — gpt-5.5 xhigh+fast-on exit 0, tokens populated.
- **MOVE M3 (RETRACTED):** Parser bug claim — over-generalized from 1-shot proxy anomaly. Primary source shows parser works. Retracted after peer cross-critique.
- **Evidence:** HYPOTHESIS/METHOD/RESULT triple for Probe 1+2.
- **Verdict:** M2 ACCEPTED; M3 RETRACTED.

### cdx-reviewer-bench-methodology
- **MOVE M4 (DROPPED — miscontextualized, not fabricated):** `service_tier` 2-variable confound — cited `run_one.sh:32-37`. File DOES exist at `~/quickdir/codex-model-throughput-bench/bin/run_one.sh` (reviewer's citation was path-sloppy, not spoofed — discovered post-round via template-provenance check). Lines 32-37 genuinely inject `service_tier="fast"` on fast_enabled arm; lines 44-46 sed-strip it on fast_disabled arm. **But that harness is `codex-model-throughput-bench`, not our xbreed `bench-cell.sh`** — our bench doesn't have this asymmetry (service_tier is constant, and codex TOML parser silently ignores it anyway per `src/config.rs:45`). Reviewer cross-applied a finding from a different bench. DROPPED for wrong-bench application, not fabrication.
- **MOVE M5 (DROPPED — contradicted by judge probe):** Prompt stdin-swallow claim — judge self-probe (`codex exec --json -m gpt-5.5 "Write a 200-word essay..."`) produced coherent 262-token essay. Empirically contradicted.
- **Surviving contribution:** Weak attestation of `ask.rs:62-65` four-flag suppression parity.
- **Verdict:** Both MOVEs DROPPED. Reviewer despawned. Post-round correction: M4's core technical claim (service_tier asymmetry in the template-source harness) was accurate for its actual target — just misapplied to xbreed.

### cdx-executor-vps-bench
- **MOVE M6:** VPS bench complete — 8 cells × n=3 = 24 rows.
- **CLAIM (refined post-conflict with connector):** gpt-5.5 fast_mode has effort-level threshold between medium and high:
  - high/xhigh fast-on: FASTER (wall −38%/−23%, comparable token counts)
  - medium fast-on: verbosity expansion (272→755 tokens, wall +22%)
  - low: inconclusive
- **Evidence:** Red-gate (local) + 24-row dataset + judge SSH spot-check at low/fast-off (12.404s, 265 tok — matches scale).
- **Blocker encountered:** VPS codex 0.123.0 rejected gpt-5.5 ("The model `gpt-5.5` does not exist or you do not have access to it") — upgraded to 0.124.0 mid-run.
- **Caveat (resolved):** Initial TSV not persisted on VPS (bench-cell.sh writes to stdout; `tee -a` hit local tmp not VPS tmp). Executor delivered full 24-row dump in follow-up message; judge persisted `/tmp/bench-gpt55-vps.tsv` on VPS with all 24 raw rows. `sha256sum` + `wc -l` verified = 24 data rows.
- **Verdict:** ACCEPTED (with TSV-persistence caveat). Confidence: high for xhigh/high; medium for medium verbosity-expansion; low/inconclusive for low effort sign.

### g-connector-bench-crosspattern
- **MOVE M7:** xhigh OOS cells (C07/C08/C15/C16) in 0424 report are xask-structural only — raw arm unlocks all 4. Flagged as future-work, NOT expanded (scope-held per `feedback_godspeed_scope_discipline.md`). Also proposed `-p xbreed` contamination warning for executor.
- **Evidence:** Cross-axis (non-executable) per Pareto schema.
- **Verdict:** ACCEPTED.

---

## Conflicts + Judge Resolution

**CONFLICT 1 (model-level pattern vs VPS data):**
- Connector initial: "gpt-5.5 fast-on faster at all tiers" (−17.1% low → −32.3% xhigh), drawn from 0424 report gpt-5.5 addendum (different env).
- Executor: VPS data shows fast-on SLOWER at medium (+22%) and low (+6.3%).
- **Resolution:** Connector drew from wrong dataset — executor's VPS data is the first-ever gpt-5.5 VPS bench. Connector retracted; corrected pattern filed: effort-level threshold between medium and high.

No other cross-model CONFLICTs (reviewer's moves spoofed — dropped before conflict-classification).

---

## Pareto Verdict Per Move

| move_id | proposer | axis | evidence | verdict |
|---|---|---|---|---|
| M1 — planner skeleton | ccs-planner-r0 | planning | primary-source reads | ACCEPTED |
| M2 — reachability | cdx-labrat | empirical | HYPOTHESIS/METHOD/RESULT + exit-0 proof | ACCEPTED |
| M3 — parser bug | cdx-labrat | empirical | over-generalized anomaly | RETRACTED |
| M4 — service_tier confound | cdx-reviewer | correctness | real file `~/quickdir/codex-model-throughput-bench/bin/run_one.sh:32-37` but wrong-bench application | DROPPED — miscontextualized |
| M5 — prompt-landing | cdx-reviewer | correctness | contradicted by judge primary-source probe (262-token essay) | DROPPED — empirically contradicted |
| M6 — VPS bench execution | cdx-executor | execution | 24 rows + judge self-verify | ACCEPTED (TSV-caveat) |
| M7 — cross-axis parity | g-connector | cross-axis | non-executable per Pareto schema | ACCEPTED |

**Evidence audit:** 5 moves with evidence; 2 DROPPED (spoof_flagged); 1 RETRACTED.

---

## Axes Final State

| axis | state | notes |
|---|---|---|
| empirical reachability | MOVED | gpt-5.5 confirmed reachable on VPS post-upgrade |
| correctness methodology | MINIMAL | reviewer spoofed; only weak ask.rs:62-65 attestation stands |
| VPS execution | MOVED significantly | 24 rows filled; codex 0.124.0 requirement discovered+resolved |
| cross-axis parity | MOVED | effort-level threshold pattern identified |
| variance control | captured | σ_off > σ_on at xhigh; fast-on more stable at high tiers |
| template fidelity | PARTIAL | table shape matches; glyph/arrow rendering pending judge render |
| VPS parity | MOVED | sync+build+install+bench all ran on VPS |

---

## Commit Delta

Round 1 lands this scribe report. No `src/` modifications in xbreed (scope-held per `feedback_godspeed_scope_discipline.md`). Future round candidate: `update-codex-remote` Makefile target per planner's M1b addendum.
